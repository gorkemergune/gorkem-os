section .data
    message db "Hello, World!", 10
    length equ $ - message

section .text
    global _start

_start:
    mov rax, 0x2000004
    mov rdi, 1
    mov rsi, message
    mov rdx, length
    syscall

    mov rax, 0x2000001
    xor rdi, rdi
    syscall
